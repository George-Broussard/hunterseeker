// Layout for Seeker-facing routes. Persona-specific chrome (nav, threshold control)
// goes here in later issues.
export default function SeekerLayout({ children }: LayoutProps<"/">) {
  return children;
}
