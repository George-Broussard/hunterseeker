// Layout for Hunter-facing routes. Persona-specific chrome (nav, company switcher)
// goes here in later issues.
export default function HunterLayout({ children }: LayoutProps<"/">) {
  return children;
}
