# Copy to .env at the repo root. Read by docker-compose (POSTGRES_*) and by apps/api (DATABASE_URL).
# .env is gitignored; never commit real credentials.

POSTGRES_USER=hunterseeker
POSTGRES_PASSWORD=hunterseeker
POSTGRES_DB=hunterseeker
POSTGRES_PORT=5432

# Async SQLAlchemy URL for apps/api. Must use the postgresql+asyncpg driver.
DATABASE_URL=postgresql+asyncpg://hunterseeker:hunterseeker@localhost:5432/hunterseeker

# Log every SQL statement (dev only).
ECHO_SQL=false

# Width of the pgvector columns on the embedding tables. Placeholder until issue #4 is decided.
EMBEDDING_DIM=1024

# Where apps/web reaches apps/api. API_URL is read server-side (server components, route
# handlers); NEXT_PUBLIC_API_URL is inlined into the browser bundle at build time.
API_URL=http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000

# Shared HMAC secret. Auth.js (apps/web) encrypts its session cookie with it AND signs the
# bearer token it sends to apps/api; the API verifies that signature with the same value.
# Generate one: `openssl rand -base64 32`. Must be identical in both apps.
AUTH_SECRET=change-me-generate-with-openssl-rand-base64-32

# apps/web only: its own public origin (Auth.js uses it for callback URLs and cookie flags).
AUTH_URL=http://localhost:3000
